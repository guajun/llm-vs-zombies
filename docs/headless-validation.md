# 后台实机验收记录

2026-09-18 至 19 日，在锁定原版 1.0.0.1051 引擎上执行。以下证据来自真实进程，未使用桌面点击、系统键鼠输入或桌面截图。游戏本体及完整实验目录仅保留在本机。

| 验证 | 实际结果 |
|---|---|
| 私有用户档与注册表 | 原生隔离 fixture 通过；实机使用每轮独立目录与进程身份 |
| 隐藏启动 | 所枚举的本进程窗口均隐藏，命名管道正常连接 |
| 参考场景 | Scene 3、26 株植物、10 张指定卡序、8000 初始阳光核验通过 |
| 初始化边界 | GameClock 3151、wave 0、refresh countdown 599 |
| 精确推进 | 连续请求 1、9、90、900 tick，合计 1000；实际 GameClock 增量逐项相等 |
| 暂停 | 间隔一秒的完整已审计快照相同 |
| 真实动作 | 1 行 8 列种小喷菇，随后铲除，均返回真实成功且各推进 1 tick |
| 断线处理 | 较早性能探针请求 90 tick 时超时；断线在第 68 tick 停止，原请求 ID 可查回实际结果 |
| 原画 | 引擎绘制表面捕获 800×600 BGR24，首帧前后已审计状态及已识别 RNG 相同 |
| 流式视频 | 31 帧原画、10 fps、3.1 秒 MP4；FFprobe 核验分辨率、帧数和时长，无丢帧 |
| 冷启动重放 | 007 在死亡预览动画句柄处分叉；012 初态和前 604 tick 一致，第 605 tick 的 CRT 随机状态分叉，仍未通过 |

主要集成探针为 `headless-launch-006`，runtime DLL SHA256 `81500a24e598e302be8e0ab2c2b43f6ea22bcc620bb1ab9a2d6a3273909aa89f`。该轮正常关闭原生记录器、终止自己的进程并封存。视频记录不重复把像素 Base64 写入 JSONL；日志保存像素 SHA256 与捕获元数据。

冷启动探针为 `eval-headless-007`，源轨迹真实推进 1000 tick，但重放在初态检查时拒绝继续。首个差异路径 `/zombies/slots/0/fields/00000118`；不能静默丢弃此差异或把失败算成通过。另一个失败检查来自启动前后前台 HWND 不同，而本进程窗口均隐藏；仅此不能确定焦点变化的来源，后续验证改为启动期间只读采样前台 PID。

这些短程通过项不代表完整两旗成功、十次冷启动一致、完整 RNG 覆盖、完整进程检查点或已达到严格实验就绪。后续修复和长程结果须产生新的独立证据，不能改写旧实验结果。

2026-09-19 更新：`headless-baseline-009` 的实际 Python 策略执行 5,000 tick，到第 3 波，12 株核心植物仍在、阳光 7950、失败动作 0。精确初始化 hook 捕获 232 次僵尸初始化，关闭时无溢出、线程错误或未结束调用。该轮证明短程真实策略可运行，不代表两旗胜利。

动画句柄规范化后，`eval-headless-012` 两次独立启动的初态一致，逐帧重放在第 605 tick 首次发现 `/rng/instances/game_thread_crt/state` 不同：3668862914 与 1119315906；其余当帧捕获字段和全局 MT 一致。原始引擎存在由对象地址派生种子的 `srand` 调用，正在定位并处理。这是实测失败，尚未满足确定性重放验收。

`headless-boundaries-013-{single,batch,render}` 使用同一 DLL `0110db8ec06980675822c95e5b26f40cd48b9401204092f6e6fd7601601d684d`、种子 42 和初始化时钟配方。100 次单 tick 与一次 100 tick 的全部 200 个前后边界语义状态一致。另一次每 10 tick 捕获原画并编码视频，与不捕获组的 200 个边界亦一致，10 次捕获全部通过 RNG/时钟检查。这个 100 tick 区段没有出生，因此报告明确 `spawn_exercised=false`；不能外推到后续出怪、特效或完整两旗。

`headless-recovery-014` 在确认请求已被接受后断开连接，100 tick 预算在实际第 2 tick 以 `client_disconnected` 停止。重连按原 ID 查询两次得到相同结果，实际时钟相符；非法动作推进 0 tick，后续合法推进 1 tick。`headless-actions-015` 另行验证了真实占用格拒绝、种植成功、冷却未完成拒绝、旧观察版本拒绝及铲除成功，正常关闭并封存。

`eval-headless-016-s42-c0` 与 `headless-replay-019` 首次通过真实冷启动重放：种子 42，1,000 tick、2,000 个 pre/post 边界的全部已捕获语义状态相同；40,946 次粒子种子转换事件逐项相同，关闭健康通过。轨迹 ID 为 `73d766842cda226cf8f459438063eb6ed7551cd4fc9682da77f96e68a5b09146`，DLL SHA256 `9e5ae28978b372d3a6a70db421a41739b5dd4f473eb5874b859d2c8aa93c5290`。这是 [明确改变粒子地址播种行为的模式](particle-shake-determinism.md)，不是未经修改原版的逐位证明，也不是完整内存确定性证明。末尾无推进 pause 也真实执行并核验状态不变。

同一 DLL 的长局 `headless-fullcycle-017` 到第 4,166 tick 主动报告 `reanimation_link_fault`，路径 `/plants/slots/65/fields/00000094`。先前没有失败动作；审计冻结后正常关闭和清理，记录以 `audit_failed_at_tick_4166` 封存。该局不能算完整两旗通过，正在核对具体植物生命周期。

修复后的 `headless-fullcycle-020` 已直接捕获第 4,166 tick 的 `mDead=false, mSquished=true`，并验证该动画句柄确已释放。前 4,500 tick 的全部 9,000 个前后边界通过完整状态摘要与原始动画旁证核验，之后继续推进。该直接证据支持窄范围的压扁植物效果退役规则；不是整局关闭或冷启动重放的通过结论。

`headless-video-021-{plain,render}` 完成了覆盖出生阶段的录像独立验收。DLL SHA256 为 `a36bf5320954a81a0d8903dae577cf54e6bdce0d91b05c8efa821cc4f53aa4e0`，两组均为种子 42，同一时钟锚点，执行 10 次 100 tick。render 组在 tick 0..900 每 100 tick 捕获一张 800×600 BGR24 原画，FFmpeg 编码后由 FFprobe 核实 10 帧、1 fps、10 秒，零丢帧、拒绝或编码失败。

两组 2,000 个 pre/post 边界的全部已捕获语义状态、40,946 次受控粒子调用、71 次受控僵尸初始化退出事件逐项一致；其中出生的初始位置、随机标量及 RNG 前后态均参与比较。两组的 34/38 个归档证据文件已封口并核验完整文件清单和 SHA256，比较后再次检查未变。原用户档指纹不变，两个自有进程正常关闭；启动和运行期的有限采样均未观察到目标进程前台归属或可见窗口。采样不构成连续焦点保证，等价结论限于此种子、区间及文档列明的捕获范围。

021 plain/render 轨迹 ID 分别为 `de7af679e54eeadede9715655eda87739614b020c95c7a7c16f8843c09c4b1a2`、`5fcc7165fe9ead14f422f8f19a4a48e441d6183f853dfa9656fb8133c09df420`。原画视频 SHA256 为 `c8ee60f1bc24f5561ef94bbd4a7fe68c95ca6483c3c738f97ff08dc28b0f0c4d`。完整本机验收报告为 `work/video-021-acceptance.json`。

`headless-actions-source-022` 另录制占用格拒绝、真实种植、冷却拒绝、等待、铲除与后续推进，合计 1,000 tick；新进程完整重放的全部边界及 39,776 次粒子调用一致，关闭健康通过。再从另一冷启动重算到 B(250)，真实铲除并种下倭瓜、继续到 B(261)，分支成功独立封包且带父轨迹身份。最后一组故意将实际种植列从 8 改为 7，在 `request[1].result` 的 `/action_results/0/action/col` 明确报告首次差异。三个流程的共享用户档指纹不变。源轨迹 ID `470c76ee7c8046663a7a839aa21604ea1924337aa4912efb6995809572b18ec8`，分支 ID `4c535622f4126a9a5b72791de9d29fbb741e736a40239c0f4012370dede45598`。

020 长局最终在 tick 12,940 / 第 6 波撞到请求/响应去重缓存的 64 MiB 门槛，下一 mutation 返回 `dedup_capacity`；同一门槛又阻止了 `stop_recording`。自有进程已停止，原始 `capture.lock` 和未封口日志保留，正常封存函数正确拒绝将其伪装为完成。该实测失败由 [#8](https://github.com/guajun/llm-vs-zombies/issues/8) 跟踪，不能算完整两旗或正常录制通过。

`headless-dedup-024` 用新 DLL `eb40028a6b65a33f054d3e5f01884bab06bd40e7491a5e1f6c97b0c16aab7d71` 验证磁盘请求历史：真实游戏连续完成 10,353 次零 tick 请求，仅返回结果正文就有 73,402,770 bytes，超过旧 64 MiB 上限。之后查询最早请求、重连按原 ID 重试均返回精确原结果；改动同 ID 的请求被拒绝。整个压力阶段完整审计快照不变，再推进一个真实 tick 成功。原生记录及请求 journal 正常关闭，归档封存和全清单校验通过，共享用户档不变，自有进程停止。这是容量和关闭路径验收，不等同于 10,353 个游戏 tick 的轨迹验证。

配套原生控制器 fixture 实际执行了 200,005 个单 tick 请求，journal 为 112,025,237 bytes，测试进程私有内存从 2,023,424 增到 2,027,520 bytes。另覆盖磁盘写入/封口失败、损坏和截断、终局故障、旧结果查询及专用关闭保留区；完整 CTest 8/8、本机 Python 162/162 通过。fixture 的单步不是原版实机长局。024 本机报告为 `work/dedup-024-acceptance.json`，原生报告为 `work/request-journal-native-acceptance.json`；完整两旗继续独立验证。

同一 `eb400…` DLL 的 `headless-recovery-027` 通过 29 项检查：先确认请求处于 pending，再断开连接，实际推进 4 tick 后以 `client_disconnected` 停止；新连接按原请求内容和 ID 重试，原结果与完整快照均不变。另一请求实际推进 3 tick 后被主动 pause 停止。非法动作、占位和冷却失败均推进 0 tick；后续合法单步、真实种植和铲除成功。全局共 10 tick / 20 个审计边界，每次中断恰有一个权威完成记录，26 个文件正常封存并校验，原用户档指纹未变。

`headless-video-028-{plain,render}` 在该 DLL 上重做 021 对照：2,000 个 pre/post 边界、40,946 次粒子调用、71 次受控出生出口事件全部一致。原画 MP4 仍为 800×600、1 fps、10 帧/10 秒，无丢帧；已解码检查实际雾夜两仪画面。36/40 个文件分别封存并通过比较前后哈希校验。轨迹 ID 分别为 `f1f2bdab639e141d90bfbaf19b90140ef651a126c92d6afc2777a152449102d2`、`9457672dee3bcebf02da5df4baabc85e5af2bff013708aaa403e86190b85ee5f`。录像 SHA256 为 `f3747b798ab16faf4cc7b81b80f5a7aeff2b718af6f9557cc869f1dda12dbf2e`。首轮验证工具遗漏了单独的解码 PNG，因此在封包前明确报错；补做解码和视觉检查后重新验收通过，原始游戏记录没有修改，工具失败保留在 `work/video-028-acceptance.json` 的历史中。

## 长程失败与绘制调度问题

`headless-fullcycle-025` 使用相同 `eb400…` DLL，实际推进 18,618 tick，到第 9 波失败，未完成两旗。请求 journal 达到 **143,324,883 bytes**，超过旧容量门槛后仍持续工作，原生记录和 journal 正常关闭、自有进程停止，普通实验归档已封存并验证。这与024和200,005次请求 fixture 一起完成了 #8 的容量/关闭验收，但不代表整局严格重放通过。

该局最后一个请求执行了一次有真实状态变化的原生调用：同一 Board 的 GameClock 没有增加，UI 进入失败界面，RNG、实体和动画等产生416处审计 Patch。原结果保留 `executed_ticks=0`、`native_tick_delta=0` 和 `tick_delta_verified=false`。严格 trajectory 封包因此正确拒绝 `invalid actual one-tick audit boundary`；普通归档的 outcome 为 `terminal_unverified_gameclock_zero`。后续应以独立调用身份支持这种终局转换，不能把它补成一个 tick 或删除末态。

`headless-fullcycle-029` 原拟验证 #9 的计划灰烬目标修正；与025的完整初始捕获状态相同，实际动作在 tick 4,765 才首次不同。然而在动作仍相同的 **post tick 1,939**，同一无头舞王的 HP 已是148/151，全局 MT 游标为580/552。这里的逐帧定位比较了指定游戏状态根及事件片段，不是重新完成两份全量旁证核验。原版随机流血代码每次扣3血，与差异吻合；额外 RNG 消耗的来源尚未证明。

用029的相同实际观察分别运行公开策略与候选策略，前2,323次决策（至tick11,160）及内部策略状态均相同，候选重定向尚未触发。因此029不能作为策略修正的因果对照。该局已在 tick13,133 / 第5波主动暂停，正常关闭并以 `interrupted_confounded_policy_comparison` 封存；journal 为85,551,989 bytes，共享用户档未变，未将中断算成完整周期。

另一独立对照 `headless-batch-030-{single,batch}` 在种子42、同初态、无动作/无捕获下执行1,000次单步与1次1,000步。两份记录均正常关闭、封包和通过归档哈希校验，但**严格等价失败**：post599全部捕获字段仍相同，下一pre599已有50只僵尸身体动画的RGB共150个字段不同。精确引擎反汇编确认原版绘制函数会写这些字段，而外层绘制调度在现有逐帧控制之外；这些颜色还会被后续更新传播到附件，不能直接从比较中删除。030实际覆盖71次受控出生，但比较在较早差异处终止，不构成71次出生全部等价的结论。

修复由 [#10](https://github.com/guajun/llm-vs-zombies/issues/10) 跟踪：暂停时禁止自主战斗绘制，初始化固定一次预热，每个经过验证的原生更新后固定一次原版绘制，视频仅取该边界缓存。此模式仍需独立构建及至少5,000 tick的单步/批量、冷启动、暂停和录像对照；不能以短程画面一致代替1939处真实HP/RNG分叉的复验。

## 固定绘制版本的独立实测

`headless-draw-031-baseline` 使用新 DLL `2fe646b7bdefc3768971a952b1b3aa56a2e64e24426c3abff375fe140f53acab`，在隐藏原版引擎中完成5,000 tick、246次策略决策、13个实际动作，失败动作0，末态为第2波且12株核心植物仍在。关闭健康记录为一次预热和5,000次受控绘制；38个证据文件正常封存并验证。轨迹 ID 为 `f8c86bb51b1b097958eb869ba5b93dd4ba89fce93ed3192c18c794ebffe3f796`。记录阶段481.74秒，包含逐帧审计、绘制和 IPC，不能当作单独的绘制开销。

其独立冷启动 `headless-draw-031-baseline-replay` 初始完整捕获状态相同，前1,343次更新的绘制回执、全部已捕获前后状态、71次受控出生及124,184次粒子事件比较通过。在下一次更新（post1,344）的绘制回执中，已识别 RNG 摘要首次不同，严格重放停止，结果为 `equal=false`。进一步读取原始证据确认：pre1,343及post1,343的完整重建状态相同，下一更新的全局 MT 游标为315/316（624个状态字相同），进入当次绘制前已分叉；两边各自的绘制前后 RNG 均不变。该更新的206条粒子调用中有四条涉及两只新粒子的不同寿命（43/40和47/41）；post1,344的其他实体字段相同。额外一次 MT 消费的调用位置仍在调查，不能仅凭回执报告位置认定由绘制造成。B(1,000)额外等待一秒的前后完整快照相同，也不能据此排除未覆盖状态随时间变化。失败记录正常关闭并保留，不能记作5,000 tick重放通过。

`headless-draw-contract-032` 则实际验证默认启动自动预热、重复缓存读取不改变捕获状态、零 tick种植后拒绝过期画面、主动推进后生成新缓存，以及非法动作的缓存失效和真实铲除恢复，共三个真实 tick。严格原生 reader 验证全部六个边界及 warm1/step3。私有验收脚本在游戏已停止后错误读取 `stop()` 回执的 `status` 键，首次验证因此报错；原始错误和清理文件均保留。后续独立只读核验确认同一创建身份的进程退出码0且进程句柄已置为有信号，正常封包和完整清单校验通过。报告明确记录这次验收工具错误及其修正，没有修改原始游戏事件。

这些运行的窗口枚举均未观察到自有可见窗口；031来源的14,786个运行期采样中前台 HWND 均为0。证据范围是该隐藏、无前台窗口环境，不代表已经实测主动焦点切换、锁屏、断开桌面会话或纯无显示设备运行。当前仍未达到严格实验就绪。

同一 `2fe646…` DLL 的 `headless-draw-031-single` 与 `headless-draw-031-batch` 分别执行1,000次单tick和一次1,000 tick，无动作、无捕获且B0与种子相同。两组的2,000个前后边界、全部已捕获状态/RNG/粒子事件、71次受控出生、一次预热及1,000次绘制回执全部一致，双方关闭健康和39个文件归档校验通过。轨迹ID分别为 `44bc6ed45556241cef2e39c074a037f3cbbaf3ba1554f898c111c2c5cdd38f19` 和 `fc8eb966dd19e0bed99e75b574d5af615a860f7c12c4baf490b330febb79bfc9`。这组复验覆盖并消除了030的第599 tick分叉；不消除031较长重放的第1,344 tick问题，后者由[#12](https://github.com/guajun/llm-vs-zombies/issues/12)独立追踪。

零GameClock终局的[#11](https://github.com/guajun/llm-vs-zombies/issues/11)已完成代码和离线审查：独立计数原版调用的进入/返回、保存同tick完整前后状态、核验原始Board地址旁证、重放终局，以及seek停在首次B(t)。主agent复核另修正了未经测量的外部Board/readiness/clock变化不能返回成功终局的问题，并保留原生故障事件。Python221/221和原生11/11通过，独立读端篡改探针也正确拒绝伪造计数、换Board和缺失终局完成事件。上述是离线验证；旧025不会被追认，新版仍需真实UI3→4零增量调用和冷重放证据。

## 随机调用诊断与真实零时钟终局

`headless-mt-033-{fast,delayed}` 使用同一个诊断 DLL `9ed2c5c1fba19e0282a516e17dd1e0c8aed76db787f54f2f5142ebdcfd9e20d1`，执行031的相同动作和预算前缀，共1,500 tick、62个请求。第二组每个请求前等待100 ms，并在B(1,000)额外等待五秒；暂停前后的完整捕获快照相同。两份已封存日志各有126,285次真实MT调用，进入、返回、写入数相等，无丢失或故障。独立读取器校验原版wrapper机器码、调用位置、实际请求归属及游标连续性后，全部调用者、参数、返回值、版本和phase序列相同。28项读取器测试由主agent复核通过。

这不是旧031冷重放通过：诊断钩子会改变耗时。033与旧031来源的首差实际发生在pre1,259→post1,260，前2,519个边界摘要一致，pre1,259完整重建状态相同；post1,260唯一字段差为MT游标487/488，624个状态字与CRT相同。033该次更新有六次来自同一原版位置的MT提取，另有一次Foley音效变体选择（range=3）。旧031没有逐调用记录，因此仍不能证明它缺失的具体调用。全MT诊断的26次`outside_controlled_call`也已逐条绑定到13次AvZ自动物品收集后的原版音效，标签本身不证明暂停期间异步更新。#12继续用更窄的音效分支观察器定位，不回滚RNG或删除状态差异。

`headless-terminal-034-source` 使用已合入#11的 DLL `b331dc1980adec6fc8604b53d87ddb93f061d72c0a0ed89fb10994b4dbb2c345`，通过26次真实铲除清空植物，随后一次推进5,000 tick。原版仍处于战斗，故终局验收按预算不足失败并正常封存。独立审计确认全部10,000个边界及5,000次普通更新完整健康：四辆陆路割草机已耗尽，但最近的左行扶梯尚未到达原版入屋边界。此结果没有触达零时钟分支，不能用来判定该分支已通过或实现失败。

同DLL的独立新局`headless-terminal-035-source`使用相同26次真实铲除，再请求一次最多10,000 tick，原版自然在5,392 tick后结束。实际完成5,393次原生调用；最后一次原始Board地址前后相同，GameClock为8,543→8,543、UI为3→4，效果时钟和MJ时钟各增加1。该调用产生1,005条真实状态patch，MT游标69→279；完整末态保留在同tick的独立调用前后记录中，没有虚增tick。重连查询和原ID精确重试返回原结果，同ID改参数被拒绝，完整快照和调用计数不变。

035来源的10,786个审计边界、真实终局转换和关闭health通过严格封包；调用进入/返回/写入数均为5,393，普通时钟步5,392，零时钟终局1，无故障，绘制为warm1/step5392。归档11,662条logger记录、540个采样状态已封存并校验，共享用户档不变，自有进程正常停止。轨迹ID为`56e906f4e84fe80382f1ff78e06905e28324b7a7c76d68c41f585f1afeaf4a05`，归档manifest SHA256为`f49c1a79bb502eb19ee00a48ba2e4c3073cb9575abaf17ea4de37f933498f1ce`。这是故意失败的终局记录与恢复验收；其冷启动重放仍需独立验证，不是两旗策略成功。

`headless-terminal-035-replay`随后以相同B331 DLL、完整相同初始化配方和捕获初態，重放原26次铲除及单个`advance(10000)`。实际进入自然终局的时刻却是5,399 tick / 5,400次调用，严格比较在`request[1].result /executed_engine_calls`拒绝5393/5400，结果为失败。两局各自都产生了真实零时钟终局，不等于两条轨迹一致。重放原生记录正常关闭、原档未变，失败归档11,673条记录和541个采样状态完整封存；用户档未变、自有进程停止。进一步对两份归档hash绑定的状态摘要扫描发现，前5,457个边界摘要相同，post2,729首次出现RNG、僵尸和动画分组差异；该扫描没有重复全量原始旁证核验，也不把最终请求结果的报告位置当作最初游戏分叉。#11的冷重放门槛及#12继续保持打开。

## 原版音频状态导致的随机消费分叉

`headless-foley-036-{fast,delayed}`使用同一纯观察DLL `206c341be21bbca4672385d262721fd0138bf61205b577693bdb7b54bb31192b`，执行相同初始化、动作和预算，共1,500 tick、62个请求。完整捕获B0、原始Foley槽历史和App更新计数1296相同。延时组的额外等待与033相同。两份日志均完整健康、正常封存，用户档未变；[窄音效探针](foley-diagnostic-trace.md)没有修改原返回、分支或随机数。该探针已合入且默认关闭。

原版IsPlaying查询首先在tick309返回1/0，但第一次实际播放准入及随机调用次数差发生在pre1343→post1344的第93次Foley调用：type1 SPLAT、原caller `0x45ee76`。进入时两边App计数2640、MT游标199、slot0引用数1、开始计数2636，即有符号elapsed=4，flags=0。原查询目标`0x5c7f40`在fast返回1，在delayed返回0；后者经过原版释放路径把该槽instance/refcount清零，前者仍保留。最近播放判断因此在fast为真并直接拒绝播放，在delayed为假并继续。后者实际在`0x515188`调用一次随机变体选择，候选[0,1,2]、range=3、游标199→200，返回index0并创建新音效。前者没有执行该随机调用。

这条因果链由真实原函数返回、释放前后槽状态、原最近播放结果、实际随机调用前后游标和原返回位置共同证明，并经独立原生代码审查。没有记录声卡内部GetStatus的HRESULT/status位，因此不能进一步声称已直接观测AL=0的内部子原因。能够确定的是：原音频状态查询返回影响了是否消费全局MT，固定seed、波表和逐帧动作不足以隔离这个外部输入。

两局前2,687个前后边界摘要相同，首个游戏状态差为post1344。重建的pre1343完整捕获状态相同；post1344只有全局MT游标315/316和粒子controlled_digest不同。036的fast末态逐字段等于旧031来源的对应末态，delayed等于旧031失败重放的对应末态。这是新诊断DLL重现旧分叉机制的证据，DLL身份仍不同，不冒称旧档同DLL严格重放通过。最终Foley调用均212次，变体选择58/59次；日志8143/8136行，分别约3.2MB。原生12/12、主agent独立ABI fixture、读取器33/33和Python221/221通过。

本机比较报告`work/031-divergence/foley-036-comparison.json`的SHA256为`ca855f8913f3beaa16c5e7ca635667bc1cd887e54f6882a0e48232899dfd4ec1`，原始因果链报告为`33c129f2c8f2c1a22c15a118276c8d2a74a74bbf2f5393abf4b6aacea7879284`，状态核对报告为`27603b5b850e0472c8067e3a0cf04aede39856f3dcf2073ef8f5278857de205a`。完整原始字节及偏移保留在私有归档。后续[#13](https://github.com/guajun/llm-vs-zombies/issues/13)采用默认关闭、显式改变实验语义的启动前空音效分配候选；仍需新轨迹的长程重放、预算及录像对照验收。


## 显式空音效分配新源（037）

#13 的 `sound_effects_allocation_none_v1` 已接通启动、初始化、完整音效状态和严格重放合同，默认仍为 `original`，不改变旧档解释。功能与修复合入后，本地完整 Python 243/243通过；[合并后CI](https://github.com/guajun/llm-vs-zombies/actions/runs/35418621031)的Linux Python与Windows原生/隔离检查全部通过。这些测试不替代实机。

第一次 `headless-silent-037-baseline` 在主线程恢复前失败，原因是候选误把不含资源段的转储长度0x35e000作为PE精确映像大小；锁定EXE实际为0x394000。原失败目录与沙箱保留。修复增加直接读取锁定文件、映射section到普通字节数组并调用已编译目标校验器的离线检查；104种Foley参数的160个非空资源引用也核对到真实.data范围，没有执行游戏或放松文件SHA要求。

新运行 `headless-silent-037-baseline2` 使用冻结 recorder `d4f8fd5ad612ff24432b12284c0bda3a7bfc95516e0ea4b396ea55e7e1e5b8ae`、bootstrap `b47f9466e5fd777a03d27c02ac59ee27a617254a5fe50dc0d963687096c4f519`、launcher `ad9f58276361226102f2ae777a8c1545dc3d61f481ba541fcda37196c1d9b601`。对应隔离源代码提交为 `d88b83f5126a5be68a047defdfde3c1cc4bbd14e`，归档含当时完整源快照；公共main还含默认关闭的Foley探针，不能用其不同DLL替代这些身份。

B0实测110组历史、880个Foley槽及32个声道均为空，保留原始变体历史、App更新计数1295和分配调用计数12。主线程恢复前receipt调用计数0，recorder attach计数0。B0音效子树SHA256为 `5aa688476f6c5a2fbb8c49a1c49da4eba953c85823b653042aa4ac4e878b64d1`；未归一化App计数或恢复音效历史。

该新来源完成5000tick、245次策略决策、零动作失败，结束第2波且核心阵型完整。严格封包10000个前后边界通过，原生调用进入/返回/写入均5000，warm1/step5000；音效分配尝试1467，错误0，补丁归属/模块固定/槽空均健康。粒子调用703886、僵尸初始化164次的关闭统计均无故障。游戏全过程采样隐藏/前台归属检查通过，自有进程正常停止，共享用户档未变。运行522.91秒，封包及归档校验380.92秒，总904.21秒。

轨迹ID `9fcbba91ada3f34ce35e45022d297a2df50feefe60562b568c3b5d8ced3ed3a0`；归档manifest SHA256 `9dc9199e829d8fda72ca9bcef51890a313d2bc2d8b60aa464314130b27d04a59`。归档11341条logger记录、501个采样状态、42个校验文件已封存。当前结论只为新模式的来源录制及封包通过；冷启动重放、不同预算/录像对照、真实终局重放和完整两旗仍待后续实机验收。


第一次同D4F8冷启动 `headless-silent-037-replay1` 在B0被严格拒绝，未执行重放动作。对完整捕获状态递归比对，唯一差异为 `/sound_effects/app_update_count`：源1295，实际1294；RNG、分配计数、全部历史/槽/参数和其他状态相同。失败档已正常封存、自有进程停止、用户档未变，manifest SHA256为 `21640600fedc9bca14c117754ffc7ff7c94c61a90c0930ce9013e21a1c17c44a`。新增[#14](https://github.com/guajun/llm-vs-zombies/issues/14)显式初始化锚点控制真实App计数，不能通过忽略字段或比较器偏移掩盖差异；旧037记录和DLL继续冻结，当前仍未实验就绪。

## 显式 App 初始计数与诊断累计值（039）

[#14 的初始化操作](app-update-anchor-native.md)已合入。257 项 Python 测试通过，14 项原生测试通过，最后一处 demo 标志字节宽度修正后定向原生测试再次通过。冻结隔离源码为 `b6f9feb`，recorder 为 `ce9bf82ed6bd21eaca371e7da0d6bc002e557c3efb5a010b05c058d0b1098bae`，bootstrap 为 `5b7d20693de24fc00c367f5386cc24e83c06659d3553402e0faa4ab59c2ab85d`，launcher 为 `88fb19dcbccf8baeef7cb2e5949eae3049beef4549dcfb223e4afec4cc91b7a2`。

`headless-appclock-039-short` 完成100 tick单批推进及200个边界封包，轨迹ID为 `3c8b8937047390b130f840e0c6764af729e734d0d2cd8c737eb4bb0c7c2267fa`，manifest SHA256为 `74c0b4d739b25a274e9cd7c1f969b9701ce96e17f84d5b61a763cdac3c578135`。同DLL冷启动 `headless-appclock-039-short-replay` 的真实App计数由1427显式设置到来源1362；两份完整锚定回执分别保留1362→1362及1427→1362，其他捕获字段在各自操作前后未变。

冷启动仍在B0拒绝，未执行重放动作：完整递归比对唯一差异为 `/sound_effects/calls`，来源14，冷启动16。两局主线程恢复前及recorder attach时的累计值均为0，App锚定前后及B0分别保持14、16。这个字段来自bootstrap私有诊断变量 `lvzSilentCalls`，不是原版字段，也不决定空音效分配函数的返回；差异发生在受控实验之前。原始App、RNG、Foley历史和其他已捕获B0状态已相同。

失败冷档正常封存、进程停止、共享用户档未变，manifest SHA256为 `1d0aa8b266723a84da28ffdf052b2bab90e51d983a3d0221851300b681d057c7`。039保持原结论。新增[#15](https://github.com/guajun/llm-vs-zombies/issues/15)为诊断累计值建立显式实验计数起点，保留绝对累计旁证与错误检测；不会重置bootstrap总量、修改原版状态或升级旧档。短程冷重放尚未通过，长程和完整两旗验收继续待完成。

## 显式诊断计数起点与录像复验（041、043）

#15 已合入，冻结隔离源码为 `3995abe`，recorder SHA256 为 `e014fb839167ef370fec2ff7b3835edde2c411b24130bce9d7a81380bd3cf50c`，bootstrap 为 `80cafa904b8b3a84158ff94a492d470366dbef95db55733925f827a891eeba86`，launcher 为 `139bd0f1084a4c9f86c95ba28841e882d280f3f01a0ac1d705b0c5d0d39f1587`。主 agent 独立运行 Python 270/270；原生 15/15 通过，独立代码审查通过，[公共 CI](https://github.com/guajun/llm-vs-zombies/actions/runs/35421840738)全部通过。原始累计不清零，每个实际边界同时记录绝对累计、实验起点及差值，并由严格读取器逐条核验。

`headless-counter-041-short` 与其独立冷启动完成100 tick、200个完整捕获边界比较；来源一次批量推进，冷重放保留相同请求。B0一秒暂停前后的完整快照相同，关闭、归档、隐藏窗口与共享用户档检查通过。来源 App 计数1295→1295，冷启动1296→1295；两次实际诊断计数起点均为12。这次实机没有覆盖不同起点值，不能把夹具中的14/16当作实机结果。该100 tick区间尚无僵尸出生。来源轨迹ID为 `004302f684ea8981c5b80798486abbcbe546c89ebfefb2f58d983833d85d4f61`。

同DLL的 `headless-counter-041-batch` 与 `headless-counter-043-video` 采用相同种子42、完整初态和初始化配方，分别执行一次1,000 tick推进，以及1,000次单tick推进加缓存录像。独立校验器核对两份封存归档、全部原始旁证、动作/预算合同及视频，比较全部2,000个前后边界、1,000次真实调用、71次受控僵尸出生、40,764次粒子调用和warm1/step1000绘制回执，全部相同。各自音效绝对累计12→58，实验计数46；归档校验前后哈希不变。

录像在tick0、100、…、900取10张缓存原画，另在B0重复读取三次，像素及回执相同；十组捕获前后的完整快照均相同。随后B0分别等待1秒和5秒，完整快照与观察均未变；没有把暂停后的像素重读算作已测试。FFprobe实际解码确认为800×600、H.264、1 fps、10帧、10秒，无丢帧；首帧已人工视觉检查雾夜场景、阵型和卡序。MP4 SHA256为 `66c2cba56965f24f00d27879f17d06c0adc77a375bc5a4eef7dfae30e57e81f8`。这是按游戏时间采样的原画录像，不会把思考暂停编码为额外静止时长。

批量组/录像组轨迹ID分别为 `72b04a8d40547d5b652a429ae2e22039bd563932ebf1218872c677ec01b5f3cf`、`fe40704952386cdd5786c07acd22520ee7654daa8cc3f9f33c899f0b73e80240`；manifest SHA256分别为 `0d14c7948f0cc1fd51068a06017a97467bc5fffc70b125d2e2a68e35a3915c0b`、`7fde15e4aa4865169b664f1dea060ab10b1fd99f7bc5db2753328e121b74e3dc`。独立完整比较报告SHA256为 `309325fb17a5af7dab0eeedd35d59bf22a5cbe974dbbff20d81d4e802dadf03e`。

这些结果覆盖当前模式的短程冷启动与1,000 tick预算/录像/暂停对照；并非未经修改原版的完整内存确定性证明，也不代表完整两旗或十次冷启动门槛通过。039及更早失败档保持原结论。

`headless-counter-041-baseline` 随后完成5,000 tick来源录制和严格封包：245次决策、无失败动作、末态第2波、12株核心植物均为300 HP，10,000个边界和全部关闭health通过。轨迹ID为 `c83b89c208b41b150e1e7d0888ef5b6a8219aa7d2608b712f7bbaf8a4e6e2b0a`，manifest SHA256为 `ddaafaf18b6fd47c1ab9983f38f5cf6547f93eb3d7558edd2c6cfd47202ff8fe`。

其冷启动 `headless-counter-041-baseline-replay` 实际执行完相同245个请求和5,000次调用，在线比较走完10,000个边界、152次受控出生（另有12次初始化历史）、703,886次粒子调用及5,000次绘制，没有报告状态分叉。然而退出初始化器时，窗口观察完整性检查失败：20,706次采样没有观察到自有可见窗口或自有前台窗口，但最大采样间隔0.691秒超过规定0.25秒，状态为 `unverified`。因此整体报告保持 `equal=false`、各项 `final_health_verified=false`；不能将其称为5,000 tick完整冷重放验收通过。

失败档正常关闭并封存，进程停止、共享用户档未变，manifest SHA256为 `b95afe48f79b84cbb896dbea6ef977d195d98cd432c2bb68db64b56e0ad29783`。[#16](https://github.com/guajun/llm-vs-zombies/issues/16)跟踪将只读观察器与耗时状态解码隔离，并保留现有覆盖阈值及原始失败报告。已经完成的游戏轨迹将另做封存后的完整旁证和关闭核验；该结果与窗口采样完整性分别报告。

后续独立封存验证已完成。校验器从原冷重放的封存trace/audit另建派生轨迹，未改输入或原失败报告；全部245个请求、13个真实动作结果、10,000个边界、152次受控出生、703,886次粒子调用、warm1/step5000回执及原始旁证逐项相同，双方全部原生关闭health通过。还将冷档249次marker之后的完整快照绑定到对应B0/post状态，档案清单和全部哈希在比较前后相同。各局另有12次初始化历史出生，只作本局验证，不冒充受控出生比较。完整状态流SHA256为 `c0a55da1853edb57a4eb6c303263a410c28ddcfb7582ee37500ac4796dc19ae3`；独立报告SHA256为 `d4cec7e98b715d70994b471152a1a78935170777b6fb1930d25e42eb845df13b`，执行743.64秒。该报告的 `simulation_evidence_equal=true`、`closure_verified=true` 与 `window_acceptance=unverified` 并存；原来的整体失败结论保持不变。

## 独立窗口观察器与新宿主验证（044）

#16的宿主修改已合入 `f9ce28c`，不修改041的三份原生模块。独立子进程在游戏启动前确认首样本，绑定PID和进程创建时间，直接保存原始JSONL与封口SHA；父进程解析日志不会占用其Python执行锁。仍要求25ms目标周期、250ms最大实际间隔，超时/缺封口/错误身份/中途可见窗口均不能通过。审查中修复了拒绝非空证据目录后仍可能改写其关闭文件的问题，回归检查确认原文件及writer lock完全不变。9项观察器专项由主agent独立通过，完整280项Python通过，[公共CI](https://github.com/guajun/llm-vs-zombies/actions/runs/35423859274)全部通过。

先前 `headless-terminal-042-source` 已在空阵型中自然完成6,163 tick、6,164次调用，最后为真实零时钟GameOver；原ID查询、重试、冲突拒绝与完整快照不变检查成功。但旧线程观察器有1.018秒采样间隙，整体验收失败，未封装严格trajectory；原始档正常关闭并封存，manifest SHA256为 `affbb589c760be24270a838299255d14c0c5e068b31bfd9d34cf81b3e8610ab2`。不能把它计作终局冷重放通过。

新宿主代码单独冻结并逐次归档。源码ZIP SHA256为 `8fffc26230bde6ec043a9bb37268b4445929e9f93efd4cb00c1e01210937702d`，源码manifest为 `3bd1dbcd24723a7666cc7919fc206d3f5978362330a75068fe11700c663275f1`，观察器模块为 `6a17510b4802caa66bc9dd6ae02e6ffbb4b0f30f3e712bd50c16d7c0cc203cb4`。与041宿主相比，除换行格式外仅evaluation和新增观察器模块不同；旧native与宿主快照均不覆盖。

`headless-counter-044-short-replay` 使用新宿主重放041的100 tick短源，200个边界、实际结果和全部关闭health比较通过。启动与运行期窗口采样通过，运行期最大间隔0.025821秒，B0一秒暂停完整状态不变；自有进程正常停止、共享用户档未变、源码和二进制哈希未变。归档manifest SHA256为 `d5002252a4d0d73c378168eda2b94e9428bc1d76888e5c8242dacf5f32e99b4f`。这只是新观察器的短程真实集成通过，长程与终局新运行仍需分别验收。

新运行 `headless-counter-044-baseline-replay` 已完成5,000 tick完整冷重放验收。原样重放041的245个请求和13个动作，10,000个前后边界、152次受控出生、703,886次粒子调用与warm1/step5000绘制回执相同；原始旁证和全部关闭health核验通过。B0暂停1秒、B1000暂停1秒、B2501暂停5秒，完整捕获状态均未改变。实际App计数1296→1295，双方音效原始累计12→1467、实验计数1455。本轮仍未实测不等的音效计数起点。

启动及运行期窗口均通过；运行期32,866个样本的最大间隔为0.0573333秒，未观察到自有可见窗口或抢占前台。主agent另对六份原始观察器文件、PID与创建时间、源码SHA、采样顺序、封口哈希、整个父作用域覆盖及归档校验独立复核，全部通过。来源、宿主、驱动、原生二进制与共享用户档未改变，自有游戏进程正常退出。完整运行耗时993.06秒。

新冷档manifest SHA256为 `64f05e6ea84c3ca7ca6cd3077f1a917a718a518b4e867ce21a79adc479b3f7d0`，完整重放报告为 `58073bd5e9407374d9d7e4a90892bb641f2e32f8399a4df69ab9e0651f2afedf`，观察器原始JSONL为 `df5ff2bdd3edff7c22dfabafa454a6fa8ccdd39b103718dd40c99f2a1ee80c5d`。这关闭了本次5,000 tick冷重放及其窗口覆盖缺口；041/042原失败档结论未改。终局冷重放、带动作的5,000 tick录像/单步对照、完整两旗和十次冷启动仍待完成，`experiment_ready=false`。

`headless-terminal-044-source` 随后通过自然GameOver来源验收：在B0实际铲除26株植物，单次请求推进最多10,000 tick，实测完成6,163个时钟步、6,164次原生调用。最后一次调用前后同一真实Board、版本tick6163不变，GameClock保持9314，进入UI4。严格轨迹包含12,328个前后边界，399,002次粒子调用；原生出生总数227包含初始化历史，不当作已跨局比较的出生数。

重新连接后查询原请求ID、原样重试均取得完全相同结果，更改参数被拒绝，完整快照和调用状态不变。全部关闭health通过；App1296→1296，音效累计12→647、实验计数635。启动及运行期窗口通过，16,685样本最大间隔0.046114秒；主agent复核原始零时钟调用、窗口六文件及完整归档通过。来源、宿主、驱动、原生模块和共享用户档未变，进程正常关闭。耗时689.76秒，轨迹ID为 `17293b8b98cf64cc138585bf41af05f1d26d7b641982b3415a25fd7550f08838`，manifest SHA256为 `96af8bee35eb0d5a3b8ede115cdaacdc82f38a2cbd84e2d7a4991a649f0dd6de`。这是有意失败的完整记录；冷启动终局重放仍待执行，不算两旗胜利或整体就绪。

## 带真实动作的5,000 tick单步与录像对照（044）

`headless-counter-044-schedule-video` 使用同一E014原生模块、种子42和初始化配方，将041 baseline的245个已记录请求逐项展开为5,000个单tick请求。原13个动作及其实际时刻、顺序、结果保留，不重新运行策略或LLM来产生动作。来源录制、封包、窗口检查通过后，独立校验器读取两份已封存归档，确认完整B(0)、10,000个前后边界、5,000次原生调用、152次受控出生、703,886次粒子调用、44次环境收集尝试、warm1/step5000绘制回执及全部关闭health相同。两局各有12次初始化历史出生，单独验证而不冒充受控出生比较。录像组另外104次marker之后的完整快照均与对应B(0)/post状态吻合。

原画在tick0、100、…、4900共取50帧；B1000正常取样后额外读取缓存三次，像素和回执一致，共53次capture RPC，额外三次不送入编码器。随后在B1000分别等待1秒和5秒，实际trace间隔为1.002569秒、5.002653秒，完整状态与观察均不变。50组捕获前后完整快照相同。该实验未在暂停结束后另行重读像素，不能把这一项算作已测。

视频通过FFprobe实际解码：800×600、H.264、1 fps、50帧、50秒，无丢帧或拒收，文件2,482,494 bytes；50帧原始像素哈希逐项绑定SessionTrace与帧映射。H.264是有损编码，不将解码像素与原始BGR逐字节相等作为结论。MP4 SHA256为 `e6e1cebdabd81876b48f0d3aba66a86c36da12e2fce128f84c3d567bce1a3757`。

两份输入归档的完整清单和哈希在比较前后相同，逐边界原始动画、粒子、调用及音效计数旁证全部按各自合同验证。音效绝对累计均为12→1467，实验计数1455；本轮没有覆盖不同原点值。来源041的既有V2窗口检查为pass；新录像组V3的21,552个样本最大间隔52.36ms，未观察到自有可见窗口或抢占前台，六份原始观察器文件、PID/创建时间、源码身份、封口和父作用域覆盖已独立核验。有限采样不等于连续焦点保证。来源、宿主、驱动、模块和共享用户档未变，自有进程正常关闭。

本机实际开销如下，均为墙钟时间；子项不应重复相加：

| 阶段 | 实测 |
|---|---:|
| 录像组录制阶段（含初始化） | 557.40秒，其中动作/推进请求累计523.76秒 |
| 录像组封包与归档校验 | 332.31秒 |
| 录像组驱动完整运行 | 896.88秒 |
| 封存后两档独立完整比较 | 671.11秒 |

归档清单包含的逻辑文件字节数为来源2,737,945,275、录像组3,953,222,538；其中包含轨迹证据副本、请求记录和旁证，不能将大小差全部归因于2.48MB视频，也不是NTFS压缩后的实际占用。请求分组、额外快照和观察器宿主也有变化，这不是隔离视频编码开销的性能基准，不能据此推算任意长局速度。

录像组轨迹ID为 `17f61b3a96c1245e674a4d55b65f310e80b76a32197c3f0f39ea1e3b2a356863`，manifest SHA256为 `b7e5dafc889aa9cc2da4fb1c6d8b986c24e91f947263667677f355c278bc94c0`；独立报告 `work/044-schedule-video-comparison.json` 的SHA256为 `c414f447f0dedc16da139a369eb39231c16ce6d665618cd3c6f150c7590a083b`，明确 `passed=true`、`schedule_video_5000_passed=true`、`api_smoke_only=false`。这组证据连同044原请求冷重放，完成#10/#12在当前显式模式下的5,000 tick局部验收，两项issue已关闭，实际覆盖旧1,344/1,939分叉区间；不会改写旧原始音频/绘制模式的失败结论。

## 真实零时钟终局冷启动重放（044）

`headless-terminal-044-replay` 随后用同一E014原生模块和冻结f9ce28c宿主，从完整相同B(0)重放来源的26次铲除及单个`advance(10000)`。官方engine replay报告 `equal=true`：双方均执行6,163个真实GameClock步、6,164次进入并返回的原生调用，其中一次为零时钟终局；完整12,328个前后边界、215次受控出生、399,002次粒子调用与原始旁证通过。各自另外12次初始化出生保留为本局历史，227条出生总数不直接当作受控比较数。

最后一次调用ID均为6164，实际同Board从UI3进入UI4，GameClock前后均9314，Controller仍为tick6163。EffectClock实际59329→59330、MjClock7459→7460，完整终局状态仍比较，未因GameClock为零增量忽略变化，也未伪造一个tick。返回 `stop_reason:scene_changed`、`terminal_kind:terminal_zero_clock_update`。测量终点为epoch3/tick6163，完成转场后的控制器观察为epoch4/tick0；终局明确跳过绘制并失效缓存，比较warm1、step6163与一次terminal skip。

原生调用、音效、绘制、粒子、出生的关闭health全部通过；音效累计双方12→647、实验计数635，App双方1296→1296。运行期28,389个窗口样本最大间隔53.30ms，启动/运行窗口检查通过，未观察到自有可见窗口或抢占前台。原来源、宿主、模块和共享用户档保持不变，重放归档正常封存，自有进程停止，完整驱动耗时1,124.81秒。

实际轨迹ID为 `ef2ae8924cd1f05f5279307f97b2c0268232218fa5109d065190d50ba2707097`，manifest SHA256为 `22e78cb2454aff3b50540fdc0bed5ffcc79d2c0cdaaf0bddd49f79d36e49c5ef`；官方 `replay-report.json` 的SHA256为 `d3d5a05de26935b5629748faf8678c019ca158d60a8153cb21eb482511420873`，主驱动验收报告为 `0ff2c1babe48748630d557036504256198204b32e64ad4848e58c683f133d841`。

随后独立只读复核通过：双方封存归档的全部文件及哈希、官方重放报告绑定、调用6164的pre/post和原始Board旁证、末态391项JSON Patch与摘要、全部关闭health，以及六份V3窗口原始文件均核验通过。该复核没有再次运行游戏或重做整段模拟比较，其作用是独立审查官方结果与终局证据。报告 `work/headless-terminal-044-cold-independent-review.json` 的SHA256为 `0523e66caf2ab5bae944d3f3e11f957ae1a126fd7d7e97211c6a7c0307c66547`。#11/#15的局部验收已完成并关闭。

这是主动清空阵型后的失败复现，不是策略通关。#9的有效策略因果对照、完整两旗、十次冷启动仍未完成，`experiment_ready=false`、`original_engine_replay_verified=false`。当前显式模式改变了原版部分行为；通过范围是已捕获状态与声明旁证，不是任意引擎、显示/音频环境下的完整原版逐位确定性。035/039/041/042等旧失败档及原始报告保持不变。

## 长局动画审计失败（045）与并行试验（047）

045 baseline 在受控 tick 55,821、第20波停止：严格审计拒绝存活旗帜僵尸的已释放旗动画句柄，返回 `audit_failed`。游戏仍为 UI3，未完成两旗或通关。失败边界的完整捕获状态和原始动画证据保留，记录已关闭、自有游戏已退出；54个文件的封存 manifest SHA256 为 `3def3a2a002993d841352989227e5ac18785bb455612b4188c229000063743c9`。原生调用已返回55,821次，成功验证55,820次，另有一次审计故障，不能将其包装为成功轨迹。

实际前后状态及锁定二进制显示：该旗帜僵尸血量90→70（最大270），原版低于三分之一血量触发掉头并掉旗；`HasObject` 从1变0，旗动画进入删除队列，但僵尸的原始句柄字段未清零，身体动画仍存活。当时#20据此实现限定于该类型、该字段及同帧掉旗状态的生命周期规则，并保留无效代数、越界、其他类型及其他动画角色的拒绝条件。后续050验证见下节；045旧失败档不改写。

047首次双进程冷重放使用冻结E014模块和041的5,000 tick来源。A完成官方完整模拟比较，但运行期窗口观察最大间隔380.44ms，超过250ms门槛；B在tick2,501等待期间因A失败而中止，不能算完整重放。B启动观察器还遇到读取 `control.json` 的 `PermissionError`，运行期最大间隔385.10ms。两局均完成记录、客户端、trace及自有游戏的关闭，宿主Job最终为空，整轮 `passed=false`。窗口证据不足不等于观察到了游戏抢焦点；并行能力及公开双worker入口仍未验收。

#21随后修复控制文件共享读取与原子替换竞争，代码和公共CI已通过，生产观察器SHA256为 `70d8da1e5342222ce4911100a6e28d0f6a1f63f9ec490baa6f4930d0eb72c454`。后续CI压力测试修正了忙读/确认机制，未改生产probe或250ms门槛。047的两次长间隔主要位于单次probe内部，但原记录不能区分Win32调用耗时与线程被调度出去的时间；不能据控制文件修复宣称该慢probe原因已解决。并行实机验收仍未通过。

## 原版掉旗生命周期夹具与冷重放（050）

#20 的新规则已在独立测试 DLL 上完成真实验证。默认构建的生产 `recorder.dll` 不含触发器；显式的 `recorder_flag_fixture` 目标在受控原版更新回调内，第1次调用原版 PutZombie，第2次调用原版 DropHead(0)，第3次继续原版更新。未手工清空句柄或改写生命周期字段。hello、原生manifest及归档明确标记 `test_fixture`，公开生产实验运行器拒绝用该身份认证就绪。

050 source 与全新 cold 均完整执行3次 `advance(1)`：全部6个前后状态边界、出生语义、绘制及原生关闭健康一致。实际同一僵尸 ID `3749249036`、旗句柄 `3581542471` 在第1次更新后仍有效；DropHead 返回时旗动画处于已死亡但尚未回收的状态；第2、3次更新后旗槽已释放，原句柄仍在，僵尸和身体动画保持存活。新审计仅将这一明确掉旗引用记为 `expired`，原始代次、同帧标志及查找失败继续保留。

独立验收核对了两份完整归档哈希、实际原生边界、9行夹具事件与call ID/版本/时钟/Board绑定、官方完整冷重放、四项关闭回执及四组V3原始观察器证据。最大窗口采样间隔26.432ms，最大单次probe耗时4.323ms，均通过现有门槛。来源manifest为 `688d0dc233e73d7053e74a04f9690a8e359bda2b5f72a6b9c01e31d8f02f8047`，cold为 `9035a81a768f12683a66399e16e818c41dced4ddab9c2573d28f8148cf2e8eac`，官方报告SHA256为 `bdc9be4479953af7d3af7c7a7702db88917a4b3f7d2f0dc43563976c9290bf20`。

新代码的16项原生测试通过；与#21观察器修复合并后的隔离源码通过328项Python测试。先前独立#20树的完整回归曾因旧观察器子进程退出而有1项失败，记录保留。049首次夹具驱动也因把空闲僵尸槽当实体读取而失败，已封存；修正筛选后才运行新的050，未改写旧档。这组夹具证明具体审计规则及其冷重放路径，不是公平策略实验、完整两旗或生产长局验收，`experiment_ready=false`。

#20已在上述050实机、独立复核及公共CI通过后关闭。它不升级045失败结果，也不替代后续生产完整两旗验证。

## 浮点初态严格比较失败（048）

`headless-parallel-048-a` 使用同一E014原生模块重放041来源，在完整B0比较即失败：没有进入任何受控更新，worker B未启动。原生记录、客户端、trace及自有进程正常关闭，失败档manifest SHA256为 `0fae461fd8704a0d56a2b59db58263c505ea3a1790594a03422a813fee04a695`；正常关闭不改变 `equal=false`。

两份初态恰有以下七处差异。浮点项列原始IEEE-754位值，不做容差比较：

| 状态路径 | 041来源 | 048实际 |
|---|---:|---:|
| `/fp_environment/x87_control` | 639（`0x027f`） | 127（`0x007f`） |
| `/board/000005d0` | 1015549280 | 1015545856 |
| `/zombies/slots/3/fields/00000124` | 1098663816 | 1098663817 |
| `/reanimations/nodes/plants~1dd2d0052~1body#0/state/anim_time_bits` | 1025806656 | 1025806654 |
| `/reanimations/nodes/plants~1dd2d0052~1body#0/state/last_frame_time_bits` | 1024685653 | 1024685652 |
| `/reanimations/nodes/plants~1dd350049~1body#0/state/anim_time_bits` | 1031544278 | 1031544276 |
| `/reanimations/nodes/plants~1dd350049~1body#0/state/last_frame_time_bits` | 1030046661 | 1030046660 |

048最早的evaluation `audit_snapshot`（seq12，字节偏移31248、行长192870）已经具有全部七处最终差异，位于本轮seed、clock restore、音效计数起点、App锚定和warm之前。041、044 cold、047 A从对应首份快照至B0均为`0x027f`；048以及使用独立夹具DLL的050两局均为`0x007f`，各局MXCSR控制位均为`0x1f80`。因此050自身的三帧一致不能证明与041兼容，也不能说此次warm把控制字从639改成127。更早launcher trace未记录FP状态，最初写入者尚未确定。

锁定二进制证实`Board+0x5D0`为雾偏移的x87运算写入，`Zombie+0x124`为原始动画速率；控制字差异对应53位与24位运算精度。原版图形初始化存在未指定FPU保存标志的调用，但这只是候选机制，不是DirectDraw或新宿主造成本次差异的实证。提取报告 `work/048-fp-evidence/summary.json` SHA256为 `9ec09959571b4f4c2596bbf1fb22a3982102fc50043f2fb7f9ebb5b6601fef80`，静态复核 `work/048-fp-independent/analysis.md` 为 `738a59410ecdcc0b95004da0cfa91584525d515b0a8171f5ab6325704485fe25`。这些是本机私有报告的身份引用；不发布游戏二进制或完整审计数据。

[#22](https://github.com/guajun/llm-vs-zombies/issues/22)的最小候选是在无Board的游戏主线程上，完成标题处理后、场景seed和`AEnterGame`前固定x87为`0x027f`、MXCSR控制位为`0x1f80`，记录真实激活前后值，并在异步加载、ready、warm、受控更新和绘制边界持续验证。上述对象随后才创建或由原版存档恢复，未发现这七项中有必须更早修复且不会重建的已捕获字段。此前加载的全局定义、缓存及PoolEffect仍未证明受此模式覆盖；不据此扩称完整内存确定性。该方案尚未实现或实机通过，须以明确新模式录制新来源、严格比较完整B0及后续重放；不在B0补改已计算值、不删除浮点字段、不升级048或其他旧失败档。

## 固定浮点模式的首次真实公开短程（051）

#22实现与#19公开并行cold worker在`work/public-cold-integration-tree`（基线`4228e62`）合并后，root执行了公开runner的真实短程。本轮不追加私有驱动，只使用公开`llm_vs_zombies.evaluation run`入口，计划为`work/public-fp-preflight-3ticks.json`（SHA256 `5310ba1d732fef0c2a377d214d56519e1927b1c893517f9937cb124e9a2aae0d`）：1个3 tick源录制、2个并行cold、1个独立recovery。离线部分为374项Python测试（56.594秒）与17项CTest（71.21秒）全部通过。

前两次真实运行分别暴露两个配对缺陷，均原档保留、不追改旧档：

- `public-fp-preflight-001`：源实验在B0暂停扰动处真实失败。原因是新模式的`audit_snapshot`每次读取都会新增一条monitor检查、主循环检查也随宿主墙钟推进，整份snapshot字节相等不再可能成立。修复后暂停探针逐字段比较已捕获模拟`state`与`version`，再单独核验激活回执未变、after侧monitor健康且`last_raw`仍在目标控制位；比较范围写入probe记录。该次未执行任何受控更新。
- `public-fp-preflight-002`：源实验本身通过（`tick_budget_exhausted`），但两个cold回执被父进程以`worker gate artifact changed/foreign`拒绝。原因是scenario门槛在`apply_recipe`重写同一个`observations/initial.json`之前记录哈希；source侧此前从不二次核验，所以这个过期哈希一直存在，而worker回执由父进程严格复核。修复后source与cold都在`apply_recipe`之后记录该门槛，并由`verify_run_artifacts`在source会话结束及每个cold attempt返回前复核run目录内全部门槛产物。

`public-fp-preflight-003`通过：`cold_starts_attempted=3`、`cold_starts_verified=3`、`failed_cases=0`，除smoke专门排除的`full_cycle`、`ten_cold_starts`、`strict_suite_not_requested`外全部门槛为pass。关键的实测值：

| 项目 | 源 | cold 1 | cold 2 |
|---|---:|---:|---:|
| outcome | `tick_budget_exhausted` | `engine_replay_completed` | `engine_replay_completed` |
| `equal` | — | true | true |
| 浮点激活回执比较 | — | true | true |
| 已核验原始FP边界 | 6条 | 6条 | 6条 |
| 关闭health核验 | 通过 | 通过 | 通过 |
| 运行期窗口最大间隔 | 26.09ms | 25.95ms | 26.31ms |
| 启动期窗口最大间隔 | 30.73ms | 30.56ms | 27.68ms |

三个进程的激活回执都是：`before.x87_control=127`、`after.x87_control=639`、`after.mxcsr_control=8064`、`activation_count=1`、`game_ui=1`、`board_address=0`、实际线程等于owner线程；关闭health均为`healthy=true`、`first_fault=null`、`wrong_thread_checks=0`、`last_raw`仍在目标控制位。每个进程的owner线程ID、loop/snapshot检查次数和sticky flags保持为本机原始旁证，不参与跨进程比较；`state`逐字段严格比较、不做容差。两个cold各自完整执行源请求并`equal=true`，实测正推进RPC交叠20.89ms（仅表示区间交叠，不表示CPU指令同时执行）。recovery按原已接受请求ID解析、失败动作推进0 tick、随后恢复推进1 tick，窗口最大间隔75.90ms（启动段）/26.01ms（运行段）。

完整身份、逐文件哈希、窗口与门槛明细见本机报告 `work/public-fp-preflight-003-review.json`；原始归档位于 `work/public-cold-integration-tree/experiments/runs/public-fp-preflight-003*`，按公开仓库规则不上传。该结果只验收这一新模式、种子42、3 tick公共入口的短程与并行管线，`experiment_ready` 仍为false；完整5000 tick、自然终局、两旗胜利与十次冷启动仍待完成，047/048及本次001/002的失败结论保持不变。
