## 验收进度（2026-09-26 四轮更新）

原始验收条款保持原样；只勾选已有真实证据的条目，未满足条款附具体缺口。

- 已合并交付：#112（阶段 A 合同/捕获点表）、#113（阶段 B 公共测量层）、#114（生命周期落盘/回执/离线校验）、
  [#115](https://github.com/guajun/llm-vs-zombies/pull/115)（阶段 C 收尾、候选来源交叉验证、精确拦截设计、D 冻结计划；merge `2e09bb9`，最终 seal 只读校验 `0a3c2d9`）。
  [#116](https://github.com/guajun/llm-vs-zombies/pull/116)（已合并至 main b1569371；含 review a4912c3 的失控死亡/宿主遥测修正。生产 store 探针、v2 严格合同、ApplyBurn 帧链、冻结窗口报告、严格 action/result 比较与单冷 D 计划均已合入；真机 D 执行已获授权，本 issue 继续跟踪实际结果）。
  **勾选口径**：以下所有 `[x]` 仅以已合并的 main（b1569371，含 #115 2e09bb9 与 #116）代码/测试证据为准；D 真机结果、首次击杀与整体验收仍未勾选，待实际轨迹与 principal 终审。
- 本轮（独立 worktree `pi-issue111-native`）：按维护者授权的 5 个 phase store、mDead store、回收 guard/commit 进入**原生无游戏生产实现与夹具**；不跑游戏、不合并。
- 复核命令：`ctest --test-dir build/cmake --output-on-failure`、`python -m unittest discover -s tests -q`（main 840 OK）、`python tools/issue111_capture_points.py --check`、`python tools/issue111_native_candidates.py check/verify`。
- 结果边界：#99/#105 首次击杀正向门槛仍不通过；D 真机未运行；本轮完成后由主审查者复审再进入 D。

---

## 目标与解耦要求

新增正式的原生测量能力，以僵尸生命周期补证作为首个使用场景。当前交付必须独立支持实验采集、封存、离线校验和短程真机验证；未来 Agent 可复用同一事实来源，降低重复开发成本。

**不实现 Agent、训练奖励、在线条件等待，也必须能完成本 issue 的全部必要验收。** 在线控制是可选消费者，不是记录器职责，不作为本任务依赖或关闭条件。同一 runtime DLL 可以容纳测量与未来在线消费者，无需拆 DLL、建立插件系统或先完成迁移。

本任务承接 #110 发现的证据缺口，不扩大其离线分析范围。已有分析在 tick 940 识别出 5 个进入死亡阶段的对象、33 个原因未知的直接消失对象；这些是待复核线索，不是新探针的预设答案。保留未知原因，不以 HP<=0、消失标志、回收函数或炮击时间接近直接判定击杀。

## 分层与依赖方向

1. 版本适配/原生探针：确认执行位置与 ABI，复制瞬时事实。迁移后归 pvz-env；经证明通用的原语可进入 AvZ 薄 fork，不要求先切换 fork。
2. 最小测量公共层：采集上下文、顺序、定长记录/有界缓冲、边界批次与健康状态。归 env，不依赖 Agent、奖励函数、实验脚本。
3. 实验记录适配器：在安全边界消费批次，序列化、写盘并绑定运行清单。归 env 的原生记录/审计能力。
4. 文件合同与无游戏校验：归 trajectory-core；不包含游戏地址、死亡规则或 Win32 依赖。
5. 实验编排与首次死亡分析：归 rollout/分析工具。决定采集配置与结论，不能反向成为原生测量层依赖。

未来在线消费者可以读取相同批次、计算特征或提出安全边界停止请求；停止由 runtime 推进控制器执行。本期只写清接入位置，不实现该消费者、不新增 until API、不自动扩展 observe()。特权审计事实不自动成为策略可见信息。

## 接手入口（旧仓相对路径）

- determinism/spawn_hook.hpp、spawn_hook.cpp、docs/determinism-spawn-hook.md：已有初始化出口探针、ABI 保存、缓冲与健康模式。
- runtime/engine_call.hpp：受控原生调用身份与失败语义。
- determinism/audit.hpp、audit.cpp：现有 drain、审计、关闭集成。
- logger/schemas/event.schema.json、logger/avz/recorder.cpp：既有事件外壳与写盘路径。
- tests/determinism_spawn_hook.cpp：独立 ABI 行为夹具。
- tools/issue99_shovel_fork.py 与 #110：既有证据、冻结窗口、只读分析入口。

先阅读实际调用方再修改；这些是导航，不是必须照搬的最终目录。不得启动第二个 AvZ 实例、复制一套推进循环或另造不兼容轨迹解析器。

## 阶段 A：合同与原生路径证据（首个独立 PR）

- [x] 复核 #110 最新报告及来源身份，列出现有证据和缺口，不修改原始报告/封存。
  - 证据：docs/issue111-生命周期测量合同.md §2；tree 复算 matches=true、4 节点、两份报告摘要与 #110 声明一致；未修改旧证据。
- [x] 提交捕获点表：事件名称、入口/出口相位、原始字段、版本/目标字节、ABI、调用者/分支依据、已覆盖与未知路径。
  - 证据：docs/issue111-捕获点表.json；`tools/issue111_capture_points.py --check` ok=true；established 行含字节/ABI/调用依据，review_required 行只登记疑点。
- [x] 分开定义初始化、确认死亡路径、非死亡移除、回收事实；ZombieInitialize 出口不能命名为调用者完成最终放置。没有依据的原因显式 unknown。
  - 证据：合同 §3 四类事实与 `fact_classes`；ZombieInitialize 出口只声明“共享 epilogue 前”，移除默认 `removal_unclassified`。
- [x] 明确死亡/击杀判据和不覆盖项；完整攻击者/具体炮弹归因不属于本期，除非是区分死亡与普通移除所必需。
  - 证据：合同 §5；`first_kill_gate` 仍不通过，State=2 不归因炮击。
- [x] 写出最小数据合同与失败语义、模块依赖和 schema 兼容办法。已有 seq 若是写盘顺序，不能冒充捕获顺序；需要独立 capture_sequence 或明确版本化迁移。
  - 证据：合同 §6–§8；`capture_sequence`/`seq` 分离、失败语义、schema v1 新增字段与旧轨迹 unavailable 规则。

**Junior 执行关卡：不要求凭空完成复杂逆向。缺少可靠地址、ABI 或死亡路径依据时，提交具体疑点和证据，由熟悉引擎的维护者审查；该关卡通过前，不实现未经确认的生产 hook，不跑游戏。** 可以继续完成不依赖这些地址的合同与缓冲夹具。文档审查通过不代表原生语义已经真机验收。

## 阶段 B：最小可复用采集与记录（第二个独立 PR）

- [x] 复用出生探针与现有 runtime 宿主，抽出实际需要的公共部分。具体事件使用明确结构/类型标记，不做泛型 hook 生成器、动态注册或脚本语言。
  - 证据：determinism/measurement.{hpp,cpp}、spawn_hook.*（PR #113，已合入）。
- [x] 公共上下文覆盖运行/场景或分支身份、实体槽位与代次、原生调用号、tick/revision/相位。初始化等不在受控调用内的事件明确标记，不能伪造 call_id；指针不作为跨运行实体身份。
  - 证据：`lvz.lifecycle-event.v1` 投影（PR #113）与持久化 envelope（PR #114）；初始化事件 `version`/`engine_call_id` 显式 null，validator 强制。
- [x] 所有参与生命周期排序的事件在捕获时进入同一顺序域；需要配对的嵌套调用保留 invocation/parent 关系。序号不能因 drain 重置。
  - 证据：共享 `capture_sequence`/`invocation_id` 与 `tests/determinism_spawn_hook.cpp` 的出口顺序、嵌套、drain/重装不重置夹具（main）。
- [x] hook 内只做有界复制；不堆分配、不序列化、不写文件、不调用 Agent 或任意订阅回调。保存适用的寄存器、标志、浮点状态和线程错误状态，不调用 RNG 或修改游戏字段。
  - 证据：出生探针入口/出口 shim 与 GPR/EFLAGS/x87/SSE 保存夹具（PR #113，已合入）。
- [x] 定义单一宿主 drain 与批次所有权：记录适配器消费不可变批次；未来消费者不得通过第二次破坏性 drain 抢走记录。缓冲复用前完成同步消费或明确复制，不暴露悬空指针。
  - 证据：`DrainSpawnBatch()` 单次破坏性 drain + `MeasurementHost` 台账（PR #113）；audit 适配器是唯一 lifecycle 写盘消费者（PR #114）。
- [x] 开启回执、覆盖起止、captured/delivered/persisted 等计数含义、溢出/错误线程/嵌套失配/未完成事件/关闭回执可校验。记录器写盘失败与采集失败均显式传播，不静默生成合格证据。
  - 证据：8 项计数器与两阶段 Close/Commit/Abort（PR #113）；原子 close receipt 与写盘/close 失败测试（PR #114）。
- [x] 安装验证、失败回滚、hook 所有权与卸载安全沿用现有约束；保留仍被 hook 引用的 DLL，不全局停用其他 hook。
  - 证据：Install/Remove 字节校验、所有权拒绝、未知签名拒绝夹具（PR #113，已合入）。
- [x] 新模式显式启用并绑定游戏/构建/探针/schema/配置身份；旧轨迹仍可读取，缺新能力标为 unavailable，而非零事件。
  - 证据：`LVZ_LIFECYCLE_RECORDING` 显式开关、manifest capability 绑定 session/build/probe/schema；validator 旧轨迹 unavailable（PR #114，已合入）。
- [x] 实验入口独立完成 enable → 推进 → drain/记录 → 最后 drain → 健康检查/关闭 → 封存。没有 Agent 服务、消费者或训练包仍正常工作。
  - 证据（PR #115 已合并）：`tools/issue111_lifecycle_experiment.py` prepare/run/check/seal/verify 复用真实 `evaluation.run_suite` 子运行身份与资源合同；测试以游戏启动边界 mock 验证。真机运行属阶段 D，本轮不跑。
  - 证据（main b1569371）：#115 提供 suite/子运行身份、run 前冻结 plan/资源合同、严格 check/seal/verify；#116 把 prepare 扩展为绑定 recording+probes+single-cold+构建 pin，并加入封存窗口输入绑定。真机运行属阶段 D，正在执行。

公共批次接口可以先是小型内部 C++ 接口，不要求稳定跨 DLL ABI，也不要求新增 Python 在线 API。磁盘沿用现有 JSONL/封存方式；同模块实现不等于职责混合。

## 阶段 C：离线验证与分析（第三个独立 PR；真机前通过）

- [ ] 无游戏夹具覆盖：同槽不同代次、同调用创建后移除、同 tick 多调用、死亡后延迟回收、未知移除、初态残留、嵌套调用及事件顺序。
  - **未勾**：main b1569371 已有同槽不同代次、延迟回收、未知移除、初态残留、嵌套/顺序与真实回收 guard→free→commit 夹具，以及 v1 init/v2 removal 的 same-call 相关性测试；但原生级“同调用创建后移除”仍不可观测，D 真机也需验证，故本条未满足。
- [x] 原生夹具覆盖未知签名拒绝、多个出口/分支、ABI 保存、容量溢出、错误线程、初始化部分失败和未完成调用；验证行为，不用源码字符串检查代替。
  - 证据（PR #115 已合并）：`tests/determinism_spawn_hook.cpp` 覆盖签名拒绝、多出口/分支、GPR/EFLAGS/x87/SSE 保存、容量溢出、错误线程、所有权、不可读出口的部分失败与未完成调用/恢复；`ctest` 32/32。
  - 证据（main b1569371）：#115 出生探针夹具 + #116 store 探针真实机器码夹具（全 GPR/EFLAGS/ESP/x87/SSE/MXCSR/LastError、TEB 有界栈帧链、受保护读取、回收链、失败注入、ApplyBurn 帧链）。
- [x] 文件校验覆盖缺事件/乱序或重复、计数失配、截断/缺关闭回执、写盘失败、schema/能力不支持；失败不得解释为“没有死亡”。合法筛选若存在须有单独计数与声明。
  - 证据：tests/test_lifecycle_events.py 与 tests/determinism_lifecycle.cpp 的负例表；严格读取器整合同一校验入口（PR #114，已合入）。
- [x] 不加载 Agent/在线模块，用纯实验记录消费者完成全链路；用无游戏批次读取示例证明消费端不必重写原生探针。不为演示编造第二种生产事件。
  - 证据（PR #115 已合并）：`examples/issue111_lifecycle_consume.py` 只依赖共享读取器；`lifecycle_report`/`lifecycle_compare`/check/seal 纯离线；无 Agent/Win32 依赖。真机 evaluation 全链路属 D。
  - 现状说明：#115 已合入消费示例与纯离线报告/比较；真实 evaluation 全链路属阶段 D，尚未在真机验证。
- [x] 提供单一离线命令，输出首次确认死亡、首次移除、未知原因和可否证明全窗口首次击杀，逐项附原始事件坐标与覆盖限制。
  - 证据（main b1569371）：`tools/issue111_lifecycle_report.py --plan` 输出边界+捕获事实、未知原因、`first_kill` 与覆盖限制；`report_for_run` 绑定冻结计划/初始根/终点并作用声明窗口，正负例测试齐全。真机窗口结果由 D 单独记录。
- [x] 通过仓库要求的适用测试/检查，报告提交、配置、命令与结果；区分离线通过与尚未真机验证。
  - 证据：PR #113/#114 正文记录构建、ctest、unittest 与合同校验结果；真机部分明确未运行。

## 阶段 D：短程真机验收（证据交付）

实施前冻结计划并按当前实验资源/运行约束排期，不干扰其他正在运行的任务。使用独立运行目录和新构建身份，不覆盖旧四轨迹、报告、seal 或 manifest。

1. 先做新增生命周期插桩关闭/开启对照：其余初态、动作、基础审计与模式一致，优先使用同一构建的模式开关；比较共同可观测状态、动作、RNG 与结果，报告首个分叉和比较范围。不能用事件流不同本身判失败，也不能声称证明了所有隐藏状态无扰动。
2. 再完成同一新模式的对照 A/B 两次独立冷启动，覆盖 #99 已冻结完整短窗口；运行前确认终点定义及 tick 硬上限，历史 tick 1201 是参考结果，不能为了复现数字改写停止规则。
3. 比较完整事件序列与健康，不只比较数量。重点调查 tick 940 对应的未知消失路径，但结论扫描整个声明窗口；新轨迹偏离旧轨迹时如实定位，不强求出现相同 33 个对象。
4. 输出新报告与封存、可复现命令、输入及工具身份。分别判定采集机制完整性、观测不扰动的已验证范围、两次重复性、首次击杀证据充分性。
5. 以上通过且路径覆盖足够后，才安排干预 A/B；属于后续四轨迹验收，不是本 issue 默认追加的运行。若缺口仍在，列出最小后续任务，不能反复扩大窗口追求正结果。

## 完成判据

- [x] A/B/C 的交付与相关负例通过，纯实验路径不依赖任何 Agent/训练/在线控制实现。
  - 证据（main b1569371）：A/B（PR #113/#114/#115）与 C（#115 边界/离线交付 + #116 生产 store 探针、v2 合同、ApplyBurn 链、严格比较）均已合并且负例测试通过；纯离线路径无 Agent/Win32 依赖。D 真机结果见下一条。
- [ ] D 有绑定版本与配置的真机报告；探针存在已知故障或记录不完整时不得关闭为已验证能力。
- [ ] 语义未知可以作为明确的能力限制，但必须说明是否满足本次补证目标。若首次击杀仍未证实，只有维护者接受明确缩减的交付范围并建立后续任务后，才可按“有限观测能力交付”关闭；#99/#105 的首次击杀正向门槛仍不通过。
- [x] 提供新增测量的简短开发说明：选择捕获点、定义载荷、接入批次、测试/验证、声明覆盖范围；不要求未来事件重新实现缓冲/身份/封存。
  - 证据（PR #115 已合并）：docs/issue111-开发说明.md。
  - 证据（main b1569371）：`docs/issue111-开发说明.md`、`docs/issue111-生命周期测量合同.md` 已包含 store 探针、捕获事实、资源所有权、ApplyBurn 链与 D 计划说明。

## 非目标

Agent 接入、奖励/训练框架、在线 until/暂停工具、任意回调事件总线、受限脚本 VM、热编译/热加载、独立插件 DLL、多版本自动适配、通用累计击杀统计、完整伤害来源链、先完成所有仓库迁移。后续在线控制可复用事实，但不得成为当前实验能力的必装依赖。

## 现有 issue 的处理

- #110 保持只读离线任务；按其自身实现合入和验收状态关闭，不在此直接宣布完成。新原生插桩与试录由本任务承接。
- #99 保留既有复现结论，新证据追加引用；旧报告不改写。
- #103 / pvz-agent-lab/pvz-env#1 迁移本能力；#104 / pvz-agent-lab/trajectory-core#1 承接格式、读取及完整性校验；具体死亡语义保留在游戏侧/分析侧。
- #105 仍要求新组件组合的公开入口验收，旧仓试录不能替代；#101/#102 引用本任务，不让 core 提取等待首次击杀补证。
- 不新开一个必须先完成的 Agent issue；等第二个实际消费者需求明确再开可选任务。

建议按 A → B → C → D 顺序提交，每个 PR 一个目的；搬迁/格式化与行为修改分开。没有证据或权限满足阶段条件时报告具体缺口，不猜地址、不伪造通过、不默默换实验规格。










